# termux folder (uncomment if you are using proot-distro)
# export PREFIX=/data/data/com.termux/files/usr

# careful - some cell phones don't support >32gb
export DISK_SIZE=32G

# Alpine requires some complexity for the password. Change this after installation!
export ROOT_PASSWORD=123

# Alpine iso for virtual machines / change also the version number in the answerfile
export ALPINE_ISO_URL=http://dl-cdn.alpinelinux.org/alpine/v3.19/releases/x86_64/alpine-virt-3.19.0-x86_64.iso
